dist: trusty

language: node_js

node_js:
  - 12

before_script:
  - npm install

script:
  - ng build --prod --base-href /epic-video-comparator-demo/ && npm t

deploy:
  provider: pages
  cleanup: true
  token: $GITHUB_TOKEN
  local_dir: dist/
  on:
    branch: dev
